'use client'

import React, { useRef, useEffect } from 'react'
import { motion, useSpring, useMotionValue } from 'framer-motion'

export default function MouseSpotlight() {
    // Temporarily disabled to fix scrolling and clicking issues
    return null
}
